name: Deploy Transmission

on:
    push:
        branches: [ "main" ]
        paths:
        - 'transmission/**'
    workflow_dispatch:

jobs:
    deploy:
        runs-on: self-hosted

        steps:
            - name: Checkout
              uses: actions/checkout@v4
            
            - name: Deploy
              run: |
                rsync -avz --del transmission ${{ secrets.HOST }}:${{ secrets.REMOTE_DIR }}/
                ssh ${{ secrets.HOST }} "\
                    export INFISICAL_DISABLE_UPDATE_CHECK=true && \
                    export INFISICAL_TOKEN=$(infisical login --method=universal-auth --client-id=${{ secrets.INFISICAL_CLIENT_ID }} --client-secret=${{ secrets.INFISICAL_CLIENT_SECRET }} --silent --plain) && \
                    cd ${{ secrets.REMOTE_DIR }}/transmission && \
                    infisical run --projectId=${{ secrets.INFISICAL_PROJECT_ID }} --path=/services/transmission --env=prod -- docker compose up -d --force-recreate \
                    "